{
  "_comment": "Drop this file into %APPDATA%\\Claude\\claude_desktop_config.json (or merge the mcpServers block into an existing config). The server is spawned by Claude Desktop as a subprocess; no port needed.",
  "mcpServers": {
    "agentic-os": {
      "command": "C:/Users/daley/.local/bin/uv.exe",
      "args": ["run", "--with-requirements", "AgenticOS/requirements.txt", "python", "-m", "AgenticOS.mcp_server"],
      "cwd": "C:/ClaudeSkills",
      "env": {
        "UV_CACHE_DIR": "C:/ClaudeSkills/.uv-cache",
        "UV_PYTHON_INSTALL_DIR": "C:/ClaudeSkills/.uv-python",
        "PYTHONPATH": "C:/ClaudeSkills"
      }
    }
  }
}
