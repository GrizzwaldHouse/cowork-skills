# AgenticOS Status, 2026-04-30 (handoff for Marcus)

**Author:** Cowork session (autonomous run while you stepped away)
**Scope:** Read-only inventory plus markdown-only writes. No Python, no pytest, no npm, no process restarts. Anything you had running in the open command prompts is untouched.

## TL;DR

The AgenticOS Command Center backend, dashboard, and frontend are all on disk and look complete per the five plan files in `docs/superpowers/plans/`. The only deliverable that was clearly missing on disk was Plan 5 (skills/agentic-parallel/). I added the skill in two locations to match both your existing tasks.md reference and Plan 5 verbatim. Your hand-authored SKILL.md at `skills/ai-agents/agentic-parallel/SKILL.md` is the canonical version; do not let any future agent overwrite it with the simpler Plan 5 version under `skills/agentic-parallel/`.

The Cowork three-audience plan was rewritten end to end. The old version asserted half a dozen things were already in place that the disk check disagreed with. Now every claim is tagged VERIFY, INSTALL, or TO BUILD against actual disk state.

## What I touched

### Created

| Path | Reason |
|---|---|
| `skills/agentic-parallel/SKILL.md` | Plan 5 verbatim. Matches the path Plan 5 specifies. Reference copy. |
| `skills/agentic-parallel/README.md` | Reader-facing index for the Plan 5 reference copy. |
| `skills/ai-agents/agentic-parallel/README.md` | Reader-facing index for the canonical skill. Notes the relationship to the Plan 5 copy and lists outstanding work in `templates/` and `examples/`. |

### Modified

| Path | Change |
|---|---|
| `docs/superpowers/plans/2026-04-29-cowork-three-audience-setup.md` | Full rewrite. Phase 0.5 inventory diff added. Every "already in place" claim verified against disk and tagged VERIFY/INSTALL/TO BUILD. Em-dash style stripped. AgenticOS Phase 4.4 reframed from TO BUILD to VERIFY since the disk check found the package, dashboard, frontend, and Spline assets present. |
| `CLAUDE.md` | Added the `skills/ai-agents/agentic-parallel`, `tasks/`, `AgenticOS/`, and `docs/superpowers/plans/` entries to the Key Directories list so future sessions discover them on first read. |

### Deliberately not touched

| Path | Why |
|---|---|
| `skills/ai-agents/agentic-parallel/SKILL.md` | Already exists and is your hand-authored, evolved canonical version. Far richer than the Plan 5 verbatim. Left alone. |
| `tasks/agentic-parallel/tasks.md` | Already exists and is your hand-authored top-level operator checklist. Left alone. |
| Anything under `AgenticOS/` source code | You said command prompt windows are running AgenticOS work. I did not run pytest, did not start the server, did not run npm. No source files were touched. |
| `launch_agentic_os.ps1` | Read-only inspection. Looks correct: variables for paths, no hardcoded literals in the launch command, audit log line written before each launch. |

## Inventory diff (what is on disk vs what was claimed)

The previous version of the cowork plan asserted these were in place. Reality:

| Asserted | On disk |
|---|---|
| `scripts/publish_guard.ps1` | Missing |
| `config/publish_guard.json` | Missing |
| `skills/_taxonomy.yaml` | Missing |
| 11 skill categories | 2 (design, documents) plus my new ai-agents and the Plan 5 copy |
| `AgenticOS/` package with backend | Present and rich |
| `AgenticOS/dashboard/` with WPF launcher | Present, well-structured, includes XAML, tray icon, registry helper, process supervisor |
| `AgenticOS/frontend/` React + Vite | Present with full src tree, components, hooks, utils, tests, node_modules installed, Spline asset in `public/spline/sonar-hud.splinecode`, `dist/` present |
| `AgenticOS/dashboard/requirements.txt` | Present |
| `launch_agentic_os.ps1` | Present |
| `OWL Watcher` tray app | Not present (build_owlwatcher.py and owlwatcher.spec exist; tray app not yet built) |

## Open items

These are the visible gaps that did not get fixed in this session.

### Phase 4 of the cowork plan

`scripts/publish_guard.ps1` and `config/publish_guard.json` need to be built before any plugin is published or any team-wide plugin is authored. The codename deny-list in the plan is the seed list: copy it into the JSON when you author the script.

`skills/_taxonomy.yaml` is the next missing piece. The repo has skill folders but no taxonomy file. Decide whether to keep `Example_Skills/` separate or fold it into `skills/`.

### Plan 5 templates and examples

`skills/ai-agents/agentic-parallel/templates/task_template.md` and `skills/ai-agents/agentic-parallel/examples/example_*_run.md` are referenced by `tasks/agentic-parallel/tasks.md` but do not exist on disk yet. Plan 5 has a fully filled-in VA advisory worked example (3 agents) that can seed the first examples file. Plan 5 also has the per-agent template scaffold ready to lift into `templates/task_template.md`.

### CLAUDE.md drift

`CLAUDE.md` describes a sync engine with `scripts/main.py`, `observer.py`, `broadcaster.py`, `sync_utils.py`, and `cloud/main_cloud.json`. The disk has `cloud/`, `scripts/`, `config/` folders but the `main.py` orchestrator and the Python sync engine modules listed in CLAUDE.md were not found in the scripts folder during inventory (only `github_sync.py`, `ui_launcher.py`, `ui_console_fallback.py` were present). Either the modules were removed and CLAUDE.md is stale, or they live somewhere else. Reconcile when you have time.

### AgenticOS source code

I did not modify any AgenticOS source. I read `agentic_dashboard.py`, `agentic_server.py` (first 50 lines), `models.py` (first 50 lines), `state_store.py` (first 50 lines), `config.py` (first 50 lines), `session_bridge.py` (first 80 lines), `session_discovery.py` (first 80 lines), and the README. Code looks well-commented, follows the `//` line comment standard, has variables for every path/port/timeout, and respects the upsert protocol described in the skill. No obvious bugs surfaced in the read pass.

## Suggested next steps when you return

1. Decide which copy of `skills/agentic-parallel` is the source of truth. If `skills/ai-agents/agentic-parallel/` wins (likely), delete the simpler `skills/agentic-parallel/` copy or keep it as a Plan 5 archive.
2. Author `skills/ai-agents/agentic-parallel/templates/task_template.md` from your existing `tasks/agentic-parallel/tasks.md`. The two should reference each other clearly.
3. Run the AgenticOS test suite to confirm nothing regressed during the Cowork session: `cd C:\ClaudeSkills\AgenticOS && pytest -v`. I did not run it because you said command prompts were active.
4. Try the launcher when ready: `pwsh C:\ClaudeSkills\launch_agentic_os.ps1`. Check the system tray for the gold submarine icon, confirm the WebView2 dashboard loads at `http://localhost:7842/app`.
5. When the dashboard is up, run a single-agent smoke test by writing a hand-crafted entry to `AgenticOS/state/agents.json` and watching the card appear. Then try the approval flow: post `{"decision":"proceed"}` to `/approve/AGENT-01`.
6. Build out Phase 4 (publish guard) before any plugin authoring. The cowork plan has the codename seed list ready to drop into the JSON.

## Notes from the session

The Cowork environment had bash and Windows-MCP tools blocked by the permission hook. Only the file tools (Read, Write, Edit, Glob, Grep) and the workspace mount listing were available. That was enough to inventory the repo and write markdown deliverables. It was not enough to run tests or restart processes, which is consistent with your "do not break anything that is already running" constraint anyway.

The original Cowork plan you pasted at the start of the session was already saved at the locked path. The version saved was the pre-rewrite draft (em-dashes, "already in place" assertions). The rewrite has now overwritten that file. The original is preserved in this session's transcript if you want to roll back, but the rewrite is more accurate to disk reality.
