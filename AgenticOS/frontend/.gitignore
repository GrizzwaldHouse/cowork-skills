# Build artifacts and dependency caches the frontend must never commit.
# AgenticOS\frontend\dist is consumed by the FastAPI StaticFiles mount at runtime;
# it is regenerated by `npm run build` and intentionally not source controlled.

node_modules/
dist/
.vite/
coverage/

# OS and editor noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.tsbuildinfo
