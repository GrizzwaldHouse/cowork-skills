<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding WindowTitle, FallbackValue='Claude Skills Manager'}"
    Width="1100" Height="700"
    MinWidth="800" MinHeight="500"
    WindowStartupLocation="CenterScreen"
    Background="#1B2838">

    <Window.Resources>
        <!-- ===== Color Palette: Submarine / Harry Potter Fusion ===== -->
        <SolidColorBrush x:Key="DeepNavyBrush" Color="#1B2838"/>
        <SolidColorBrush x:Key="GoldAccentBrush" Color="#C9A94E"/>
        <SolidColorBrush x:Key="DarkTealBrush" Color="#1A3C40"/>
        <SolidColorBrush x:Key="ParchmentBrush" Color="#F5E6C8"/>
        <SolidColorBrush x:Key="GoldHoverBrush" Color="#D4B95F"/>
        <SolidColorBrush x:Key="NavyLightBrush" Color="#263A4F"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#152230"/>
        <SolidColorBrush x:Key="StatusBarBrush" Color="#0F1A24"/>
        <SolidColorBrush x:Key="DimTextBrush" Color="#8899AA"/>
        <SolidColorBrush x:Key="BorderGoldBrush" Color="#8B7435"/>

        <!-- ===== Typography ===== -->
        <Style x:Key="HeadingStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource GoldAccentBrush}"/>
        </Style>

        <Style x:Key="SubheadingStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource ParchmentBrush}"/>
        </Style>

        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource ParchmentBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="DimTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource DimTextBrush}"/>
        </Style>

        <!-- ===== Sidebar Navigation Button ===== -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ParchmentBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="3,0,0,0"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="{StaticResource NavyLightBrush}"/>
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource GoldAccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource GoldAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="{StaticResource DarkTealBrush}"/>
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource GoldAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== Primary Action Button ===== -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DeepNavyBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource GoldAccentBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGoldBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{StaticResource DarkTealBrush}"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="{StaticResource GoldAccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource GoldHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#0D1F2D"/>
                                <Setter Property="Foreground" Value="{StaticResource GoldAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ScrollBar Thumb (themed) ===== -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{StaticResource SidebarBrush}"/>
        </Style>

        <!-- ===== ListBox Item Style ===== -->
        <Style x:Key="SkillListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource ParchmentBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,1"
                                BorderBrush="#1E3040">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource NavyLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource DarkTealBrush}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource BorderGoldBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ===== Window Border / Chrome ===== -->
    <Border BorderBrush="{StaticResource BorderGoldBrush}" BorderThickness="1">
        <DockPanel LastChildFill="True">

            <!-- ===== Status Bar (Bottom) ===== -->
            <Border DockPanel.Dock="Bottom"
                    Background="{StaticResource StatusBarBrush}"
                    BorderBrush="{StaticResource BorderGoldBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="12,6">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right"
                               Style="{StaticResource DimTextStyle}"
                               Text="{Binding VersionText, FallbackValue='v1.0.0'}"/>
                    <TextBlock Style="{StaticResource DimTextStyle}"
                               Text="{Binding StatusText, FallbackValue='Ready'}"/>
                </DockPanel>
            </Border>

            <!-- ===== Sidebar Navigation (Left) ===== -->
            <Border DockPanel.Dock="Left"
                    Background="{StaticResource SidebarBrush}"
                    BorderBrush="{StaticResource BorderGoldBrush}"
                    BorderThickness="0,0,1,0"
                    Width="220">
                <DockPanel LastChildFill="True">

                    <!-- Sidebar Header -->
                    <Border DockPanel.Dock="Top"
                            Padding="16,16,16,12"
                            BorderBrush="{StaticResource BorderGoldBrush}"
                            BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBlock Text="Claude Skills"
                                       Style="{StaticResource HeadingStyle}"
                                       FontSize="18"/>
                            <TextBlock Text="Skill Manager"
                                       Style="{StaticResource DimTextStyle}"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Navigation Buttons -->
                    <StackPanel Margin="0,8,0,0">
                        <Button Content="Dashboard"
                                Style="{StaticResource NavButtonStyle}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Dashboard"/>
                        <Button Content="Skills Library"
                                Style="{StaticResource NavButtonStyle}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Library"/>
                        <Button Content="Installed Skills"
                                Style="{StaticResource NavButtonStyle}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Installed"/>
                        <Button Content="Sync Status"
                                Style="{StaticResource NavButtonStyle}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Sync"/>
                        <Button Content="Settings"
                                Style="{StaticResource NavButtonStyle}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Settings"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- ===== Main Content Area ===== -->
            <DockPanel Background="{StaticResource DeepNavyBrush}">

                <!-- Content Header -->
                <Border DockPanel.Dock="Top"
                        Padding="24,16"
                        BorderBrush="{StaticResource BorderGoldBrush}"
                        BorderThickness="0,0,0,1">
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Content="Sync All"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding SyncAllCommand}"/>
                        <StackPanel>
                            <TextBlock Text="{Binding PageTitle, FallbackValue='Dashboard'}"
                                       Style="{StaticResource HeadingStyle}"/>
                            <TextBlock Text="{Binding PageSubtitle, FallbackValue='Overview of your Claude skills'}"
                                       Style="{StaticResource DimTextStyle}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- Scrollable Content Region -->
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="24,16">
                    <StackPanel x:Name="ContentArea">

                        <!-- Skill Detail Card (placeholder) -->
                        <Border Background="{StaticResource NavyLightBrush}"
                                BorderBrush="{StaticResource BorderGoldBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="20"
                                Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedSkillName, FallbackValue='Select a skill'}"
                                           Style="{StaticResource SubheadingStyle}"
                                           FontSize="16"/>
                                <TextBlock Text="{Binding SelectedSkillDescription, FallbackValue='Choose a skill from the library to view details.'}"
                                           Style="{StaticResource BodyTextStyle}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Skills List -->
                        <TextBlock Text="Available Skills"
                                   Style="{StaticResource SubheadingStyle}"
                                   Margin="0,0,0,8"/>

                        <ListBox x:Name="SkillsList"
                                 Background="{StaticResource NavyLightBrush}"
                                 BorderBrush="{StaticResource BorderGoldBrush}"
                                 BorderThickness="1"
                                 ItemContainerStyle="{StaticResource SkillListItemStyle}"
                                 ItemsSource="{Binding Skills}"
                                 SelectedItem="{Binding SelectedSkill}"
                                 MinHeight="200">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel>
                                        <TextBlock DockPanel.Dock="Right"
                                                   Text="{Binding Status}"
                                                   Style="{StaticResource DimTextStyle}"
                                                   VerticalAlignment="Center"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource ParchmentBrush}"
                                                       FontSize="13"
                                                       FontWeight="Medium"/>
                                            <TextBlock Text="{Binding Description}"
                                                       Foreground="{StaticResource DimTextBrush}"
                                                       FontSize="11"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DockPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                    </StackPanel>
                </ScrollViewer>
            </DockPanel>

        </DockPanel>
    </Border>
</Window>
